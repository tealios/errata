import type { WritingPlugin } from '@/server/plugins/types'

const plugin: WritingPlugin = {
  manifest: {
    name: 'valid-plugin',
    version: '1.0.0',
    description: 'A valid test plugin fixture',
  },
}

export default plugin
